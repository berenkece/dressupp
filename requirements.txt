# --- Prototip (Aşama 1) ---
streamlit>=1.30
pillow>=10.0
numpy>=1.24
scikit-learn>=1.3        # k-means renk çıkarımı
rembg>=2.0               # arka plan kaldırma (hazır U²-Net)
onnxruntime>=1.16        # rembg backend

# --- ML / eğitim (Aşama 2+) ---
torch>=2.1
torchvision>=0.16
# transformers>=4.46 varsayılan olarak trust_remote_code modellerini "meta"
# cihazda başlatıyor; Marqo/marqo-fashionSigLIP'in özel __init__ kodu (src/tagging.py)
# bunu desteklemediği için "Cannot copy out of meta tensor" hatasıyla çöküyor.
transformers>=4.35,<4.46 # Hugging Face modelleri
ftfy>=6.1                # Marqo/marqo-fashionSigLIP'in trust_remote_code bağımlılığı
open_clip_torch>=2.24    # Marqo/marqo-fashionSigLIP'in trust_remote_code bağımlılığı
datasets>=2.14           # Marqo/polyvore kombin veri setini yüklemek için (train_compat_model.py)
pandas>=2.0
matplotlib>=3.7

# --- Kombin uyumu / arama (Aşama 3+) ---
faiss-cpu>=1.7           # embedding benzerlik araması

# --- Geliştirme ---
jupyter>=1.0
